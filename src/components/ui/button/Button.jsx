import "./Button.css"
const Button = ({children}) => {
  return (
    
        <button className="btn content-download button-main button-scheme">
{children}
        </button>
    
  )
}

export default Button